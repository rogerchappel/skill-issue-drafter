# Safety Notes

This project is intentionally dry-run only. Generated Markdown is a draft, not an external action.

Future connector adapters must require explicit approval before creating or updating issues, and should include the generated body in the approval prompt.
